# Security

The tool makes read-only public market-data requests and writes only beneath `docs/data/`. It does not require credentials, place orders, connect to a broker, execute downloaded code or transmit local documents. The published page is entirely static; user inputs (ticker, amount, date) never leave the browser.

Report vulnerabilities privately through the repository security-advisory interface.
